---
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';

const today = new Date();
---

<footer>
	<div class="inner">
		<div class="brand-block">
			<p class="label">{SITE_TITLE}</p>
			<p class="desc">{SITE_DESCRIPTION}</p>
		</div>
		<div class="contact-block">
			<p class="label">Contacto</p>
			<a href="tel:+573002892012">+57 (300) 289-2012</a>
			<a href="mailto:info@noemec.net">info@noemec.net</a>
		</div>
	</div>
	<div class="legal">
		&copy; {today.getFullYear()} {SITE_TITLE}. Todos los derechos reservados.
	</div>
</footer>
<style>
	footer {
		background: rgba(17, 12, 11, 0.9);
		border-top: 1px solid var(--surface-border);
		padding: 4rem 0 3rem;
		margin-top: 6rem;
	}
	.inner {
		width: min(1100px, calc(100% - 3rem));
		margin: 0 auto 2rem auto;
		display: flex;
		gap: 3rem;
		flex-wrap: wrap;
	}
	.label {
		font-weight: 700;
		letter-spacing: 4px;
		text-transform: uppercase;
		color: rgba(255, 210, 200, 0.75);
		margin-bottom: 1rem;
	}
	.desc {
		max-width: 28rem;
		margin: 0;
		color: rgba(255, 232, 223, 0.75);
	}
	.contact-block a {
		display: block;
		margin-bottom: 0.4rem;
		color: rgba(255, 232, 223, 0.85);
		text-decoration: none;
	}
	.contact-block a:hover {
		color: var(--accent-strong);
	}
	.legal {
		width: min(1100px, calc(100% - 3rem));
		margin: 0 auto;
		font-size: 0.85rem;
		letter-spacing: 2px;
		text-transform: uppercase;
		color: rgba(255, 210, 200, 0.55);
	}
	@media (max-width: 720px) {
		footer {
			padding: 3rem 0 2.5rem;
		}
	}
</style>
